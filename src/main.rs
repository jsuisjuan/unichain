use chrono::NaiveDate;
use unichain::*;

fn main() {
    println!("Welcome to your UniChain!");
    // log my personal data
    let owner: (i64, String, String) = (
        generate_id(),
        String::from("Juan Carvalho Silva de Lima"),
        String::from("juanc.s.delima@gmail.com"),
    );


}
